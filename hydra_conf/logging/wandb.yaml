logging_params:
  val_tokens_processed: ${training.training_params.val_tokens_processed} #2^23
  log_step: 8
  wandb_log_step: 1
  val_step: 256
  save_ckpt_step: 500
  load_ckpt_step: 0
  keep_last: 2
  ckpt_dir: ""  # "dap_outputs/checkpoints"
  results_dir: "dap_outputs/hydra-results/my_experiment"
  wandb:
    project: "dap"
    name: ${model.name}_${data.dataset.name}_${optimizer.optimizer_params.name}_${paths.run_name}